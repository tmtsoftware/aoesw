```plantuml
!pragma teoz true
!include_many participants.puml
!includesub common.puml!variables
hide unlinked

ocs -> aosq ++: preset
aosq -> ism ++: select(port, yoffset, xOffset)
note left: offset ISM away from instrument
return Completed

group In parallel
  group NFIRAOS
  note across: See NFIRAOS Preset diagram
  end
  group LGSF
  note across: See LGSF Preset diagram
  end
  group OIWFS, for each probe used
   note across: See IRIS Preset diagram
  end
  group IRIS ODGW (if used)
  note across: See IRIS Preset diagram
  end
  group RPG
  note across: See RPG Preset diagram
  end

aosq -> aosq : wait for telescope\nmove to complete

aosq -> ism ++: select(port)
note left: point ISM to instrument
return Completed

end

return Completed

```