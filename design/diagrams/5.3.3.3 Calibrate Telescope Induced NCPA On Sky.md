```plantuml
!pragma teoz true
!include_many participants.puml
!includesub common.puml!variables
hide unlinked

ocs -> aosq ++: preset
aosq ->o? : See procedure 5.4.1 [S2]
note left: Preset to NGS star
return Completed

ocs -> aosq ++: CalibrateTelescopeInducedNcpaOnSky

aosq -> rtc ++: mode
note left: Put NFIRAOS in LGS AO mode with system flats calibrated with HRWFS in daytime
return Completed

!includesub common.puml!AcquireLGS

aosq -> ism ++: select
note left: Point ISM to NSEN
return Completed

aosq -> nsen ++: ACQ_expose
note left: Take NSEN ACQ exposure
return Completed

opt If needed
$offset("Position star on HRWFS")
end

!includesub common.puml!AcquireHRWFS

loop For one or more NGS

!includesub common.puml!AcquirePWFS

aosq -> rpg ++: calibrateNcpa
note left: Adjust LGS WFS reference vector
return Completed
aosq -> rpg ++: calibrateNcpa
note left: Record changes in LFS WFS reference vectors and averaged PWFS gradients
return Completed
end

return Completed
```