```plantuml
!pragma teoz true
!include_many participants.puml
!includesub common.puml!variables
hide unlinked

alt Daytime Calibration
aosq -> lgsf++: setState(READY-CALIB)
note left: Set LGSF state to READY-CALIB
else On-Sky Observing
aosq -> lgsf: setState(READY)
note left: Set LGSF state to READY
end

aosq -> power ++: datum
note left: turn on power to VCAMs and VNW FSM

aosq -> dm  ++: datum
note left: Turn on DM high voltage
return Completed
aosq -> dm ++: $TBD
note left: Self-test DM electronics
return Completed
& return 
& return



```
