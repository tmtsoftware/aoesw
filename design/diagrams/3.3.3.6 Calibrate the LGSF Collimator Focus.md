```plantuml

!pragma teoz true
!include_many participants.puml
!includesub common.puml!variables
hide unlinked

' TODO fix up OCS interaction

!includesub common.puml!SlewToZenith
ocs -> aosq ++: CalibrateLGSFCollimatorFocus
aosq -> lgsf ++: setState(OBSERVATION)
note left: Put LGSF in OBSERVATION state
return Completed
loop For various elevations
!includesub common.puml!AcquireLGS
aosq -> rtc ++: calibModePixel
note left: RTC sends averaged pixels to RPG
return Completed
aosq -> rpg ++: $TBD
note left: RPG computes average LGS spot size
return Completed
aosq -> lgsfFocus ++: follow
note left: LGSF focus controller uses spot size to optimize collimator spot size
return Completed
end
return Completed

```