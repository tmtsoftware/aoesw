```plantuml
!pragma teoz true
!include_many participants.puml
!includesub common.puml!variables
hide unlinked

ocs -> aosq ++: SuspendAO

group In parallel

group LGSF
aosq -> lgsf ++: setState(READY)
note left: Set LGSF state to READY
return Completed
end

group NFIRAOS
  note across: See NFIRAOS Suspend AO diagram
end

group OIWFS
  note across: See IRIS Suspend AO diagram
end

group ODGW
  note across: See IRIS Suspend AO diagram
end
return Completed
```